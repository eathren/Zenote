import { Button } from "antd"
import styles from "./NodeControlBar.module.css"

const NodeControlBar = () => {
  return (
    <div className={styles.control__body}>
      <Button> Click </Button>
    </div>
  )
}

export default NodeControlBar
